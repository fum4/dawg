{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "instructions",
  "projectType": "library",
  "sourceRoot": "libs/instructions/src",
  "implicitDependencies": ["shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsgo --noEmit -p libs/instructions/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec oxlint libs/instructions/src"
      }
    }
  }
}
